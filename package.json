{
  "name": "deckards-orchestrator",
  "version": "3.0.0",
  "description": "Deckard's Orchestrator MK3 — a seeded generative record maker",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "test": "node --test 'src/**/*.test.ts'",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^5.9.3"
  }
}
